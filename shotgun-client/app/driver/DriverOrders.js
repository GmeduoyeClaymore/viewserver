import React from 'react';
import {Text} from 'native-base';
import {connect} from 'react-redux';

const DriverOrders = () => {
  return <Text>Driver Orders</Text>;
};

export default connect(
)(DriverOrders);
